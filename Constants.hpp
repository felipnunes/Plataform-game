#pragma once
class Constants {
    public:
        const static unsigned int WINDOW_WIDTH = 1920;
        const static unsigned int WINDOW_HEIGHT = 1080;
        const static unsigned int FRAME_RATE = 60;
};